import { StyledSection } from "./style"

export const AdminHeader = () => {
    return (
        <StyledSection>
            <h1>Bem Vindo ao painel de Administrador</h1>
        </StyledSection>
    )
}